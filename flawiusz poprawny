def f(n,s):
    lista = [i for i in range(1,n+1)]
    s -= 1
    usun = s % len(lista)
    while len(lista) > 1:
        lista.pop(usun)
        usun = (usun + s) % len(lista)
    return lista[0]
print(f(10,3))
